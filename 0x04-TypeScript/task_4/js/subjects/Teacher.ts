// task_4/js/subjects/Teacher.ts

// Define a namespace to encapsulate related subject entities.
namespace Subjects {
  // Define the base Teacher interface.
  export interface Teacher {
    firstName: string;
    lastName: string;
  }
}
